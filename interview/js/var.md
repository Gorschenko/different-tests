// Функциональная, а не блочная область видимости
// Hosting - всплывает
// Переобъявляется

console.log(a); // undefined (не ошибка!)
var a = 5;

// Это работает как:
var a; // объявление поднято
console.log(a); // undefined
a = 5; // инициализация осталась
